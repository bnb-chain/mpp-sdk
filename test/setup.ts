// Per-worker setup — runs before each test file.
// Use this for vitest matchers, console-capture, etc. Reset state between
// test files via beforeEach/afterEach inside individual suites, not here.
