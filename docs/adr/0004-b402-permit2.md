# ADR 0004 — b402 Permit2 payments (`permit2-exact` / `permit2-upto` on the x402 wire)

- **Status:** Accepted for `permit2-exact` — IMPLEMENTED 2026-07-02
  (`src/b402/Permit2.ts`: `buildPermit2ExactPayment` / `recoverPermit2ExactPayer` /
  `isPermit2PaymentPayload` / `CURATED_B402_SPENDERS`; demos: the example
  server's `/x402/premium` route + the example client's `x402 · Permit2`
  browser tab). The wire spec below was
  resolved from the official b402 "Permit2 Signing Guide" (captured 2026-07-02):
  domain `{name:'Permit2', chainId, verifyingContract}` (NO version), witness is
  exactly `Witness { address to; uint256 validAfter }`, payload field
  `permit2Authorization`, all numerics decimal strings on the wire.
  `permit2-upto` stays OUT OF SCOPE — b402 documents its witness as
  contact-the-team only. Extends [0002](0002-settle-adapter.md) and instantiates
  the standalone-x402 rail sketched as Phases 2-4 in
  [0003](0003-payment-offer-layer.md).
- **Scope:** Let a buyer pay a b402 (Binance OnchainPay, x402 v2) endpoint with a
  **Permit2** signature — b402 broadcasts and pays gas — for tokens that either lack
  EIP-3009 entirely (USDT/USDC) or whose EIP-3009 door doesn't match (see the testnet
  `$U` facts below). This is a **standalone x402-wire** feature (`X-PAYMENT` header),
  NOT a change to the mppx charge wire or to `B402Adapter`.

## Why this cannot ride the mppx wire (restating the XOR theorem)

A Permit2 `PermitWitnessTransferFrom` signature binds BOTH the `spender` (enforced
on-chain as `msg.sender`) and the `witness` struct:

- **mppx `permit2` credential**: spender = the merchant's settlement signer,
  witness = `PaymentWitness(bytes32 challengeHash)` (`src/protocol/TypedData.ts`).
- **b402 permit2**: spender = b402's per-scheme proxy (`extra.spenderAddress`),
  witness = b402's own struct (facilitator/recipient binding — exact typestring TBC,
  see open questions).

One signature cannot satisfy both. Therefore `B402Adapter` (the mppx settle backend)
stays **EIP-3009-only**, and b402 Permit2 lives on the x402 wire where the buyer signs
b402's shapes knowingly. This was decided in ADR-0002/0003 and is not reopened here.

## Ground truth — live `/supported` (BSC Testnet, 2026-07-02)

| network   | scheme / method       | extra.name   | v   | spenderAddress                               |
| --------- | --------------------- | ------------ | --- | -------------------------------------------- |
| eip155:97 | exact / eip3009       | `U`          | 1   | —                                            |
| eip155:97 | exact / permit2-exact | `U`          | 1   | `0x45481A7FaFc1e62Bb7D851645927E32a2FFA0271` |
| eip155:97 | upto / permit2-upto   | `U`          | 1   | `0xA705d91f1e9f747936Fc131C92B5366C214435B0` |
| eip155:97 | exact / permit2-exact | `USDT Token` | 1   | `0x4548…0271` (same)                         |
| eip155:97 | upto / permit2-upto   | `USDT Token` | 1   | `0xA705…35B0` (same)                         |
| eip155:97 | exact / permit2-exact | `USD Coin`   | 1   | `0x4548…0271` (same)                         |
| eip155:97 | upto / permit2-upto   | `USD Coin`   | 1   | `0xA705…35B0` (same)                         |

signers: `{ "eip155:*": ["0x5f77eE41BaffDAe61830eF9be76541444FAE5D11"] }`

Verified on-chain the same day:

- `0xC70b8741b8b07A6d61E54fd4B20F22fa648E5565` ("United Stables"/`U`/18): EIP-712
  domain **name "United Stables", version "1"** (reconstructed from
  `DOMAIN_SEPARATOR()` — cryptographic match, not assumed); `authorizationState` /
  `transfer` / `approve` all publicly callable.
- The b402 eip3009 kind advertises domain name **"U"** — it does NOT match
  `0xC70b` ("United Stables") — so `0xC70b` holders **cannot** use the eip3009 path.
  Permit2 doesn't consume the token's EIP-712 domain at all (signatures are over the
  canonical Permit2 contract's domain), so Permit2 is the road for this token.
- `USDT Token` = PancakeSwap TEST_USDT (`0x337610…34dDd`, faucet-obtainable) — meaning
  the permit2 path is testable without scarce `$U`.

## Design

### Wire model (names TBC against b402 docs — see open questions)

Extend the x402 types with a discriminated payload:

```ts
// Types.ts — payload becomes a union discriminated by accepted.extra.assetTransferMethod
export interface Permit2EvmPayload {
  readonly signature: `0x${string}` // PermitWitnessTransferFrom EIP-712 sig
  readonly permit: {
    readonly permitted: { readonly token: `0x${string}`; readonly amount: string }
    readonly nonce: string // unordered 256-bit
    readonly deadline: string // unix seconds
  }
  readonly witness: Record<string, string> // b402's witness fields (typestring TBC)
}
export type PaymentPayloadBody = ExactEvmPayload | Permit2EvmPayload
```

`FacilitatorRequest.settleAmount` (already reserved in `Client.ts`) carries the actual
settle amount for `permit2-upto` (`settleAmount ≤ permit.permitted.amount`).

### Buyer builder — `buildPermit2Payment` (browser-safe, `@bnb-chain/mpp/b402`)

```ts
buildPermit2Payment({
  account,                    // viem LocalAccount / wallet-backed signer
  requirements,               // the chosen accepts[] entry (permit2-exact | permit2-upto)
  trustedSpenders,            // REQUIRED allowlist — see security model
  maxPermitted?,              // REQUIRED for permit2-upto; ceiling in atomic units
  deadline?,                  // default now + maxTimeoutSeconds
})
```

### Security model (the reason this was gated)

The buyer **cannot** call `/supported` (it is RSA-credentialed, merchant-only), so the
402's `spenderAddress` / witness fields are attacker-controllable from the buyer's
seat. "Sign whatever the challenge declares" is a phishing primitive: a Permit2
signature to a hostile spender is a direct token-theft instrument. Hence, fail-closed
rules enforced by the builder:

1. **Spender allowlist — no default-trust.** `trustedSpenders` is a REQUIRED
   parameter; the builder refuses a `requirements.extra.spenderAddress` outside it.
   The SDK ships a **curated b402 spender table** (like the curated token matrix,
   dated + per network: today `eip155:97` exact→`0x4548…0271`, upto→`0xA705…35B0`)
   that callers may pass explicitly — but never implicitly.
2. **Witness allowlist.** Only b402's known witness typestring is signable; the
   builder constructs the witness itself from `requirements` (`to` MUST equal
   `payTo`; facilitator field MUST be in the curated signer set). Server-supplied
   witness blobs are never signed verbatim.
3. **Amount caps.** `permit2-exact`: `permitted.amount` MUST equal
   `requirements.amount` exactly. `permit2-upto`: `permitted.amount = min(requirements
ceiling, maxPermitted)` and `maxPermitted` is required — an uncapped upto-permit is
   refused.
4. **Deadline cap.** `deadline ≤ now + maxTimeoutSeconds + 10min` slack; longer
   windows are refused (a long-dated permit is an off-protocol spend authorization).
5. **Validator parity.** `isEip3009PaymentPayload` stays; add
   `isPermit2PaymentPayload` with the same full-shape rigor (envelope, scheme/method
   discriminant, hex/decimal formats) for merchant-side gating of `X-PAYMENT`.

### Merchant side

`B402Client.verify/settle` already forward arbitrary `FacilitatorRequest`s — only the
TypeScript types widen. The standalone-x402 endpoint recipe in `docs/b402.md` gains the
permit2 variant (choose kind → echo `extra` verbatim → verify → settle with
`settleAmount` for upto).

### Demo plan (historical)

This plan was written against the old 4-example layout. The examples were
since consolidated into `examples/server` + `examples/client`, and the
"stretch goal" browser tab is now the shipped buyer.

- **merchant-demo** (now `examples/server`): add an x402 route (`/x402/premium`) active in mode 3 — speaks pure
  x402 (402 JSON body with `accepts[]` built from `/supported` kinds; `X-PAYMENT` in,
  `X-PAYMENT-RESPONSE` out). ~60 lines; demonstrates the merchant recipe for real.
- **client-demo** (since removed in favor of the browser tab): add `start:x402` — buyer picks a permit2 kind, one-time
  `approve(Permit2, …)`, `buildPermit2Payment`, pays. Works with TEST_USDT (faucet) and
  `$U` (`0xC70b`).
- **charge-demo** (now `examples/client`): browser support moved from stretch
  goal to the shipped `x402 · Permit2` tab (wallet-signed Permit2 against
  b402's spender).

## Explicit non-goals

- Bridging mppx `permit2` credentials to b402 (impossible — XOR theorem above).
- Auto-selecting between mpp and x402 rails in `pay()` (that is ADR-0003 Phase 2/3,
  gated on the `PaymentIntentStore`).
- `permit2-upto` subscription/metering flows beyond a single capped settle.

## Open questions

1. ~~Exact Permit2 payload field names + witness typestring~~ — **RESOLVED**
   (2026-07-02) from the official Permit2 Signing Guide; see Status. Mainnet
   spenders observed live: exact `0x3038f7ac3b4D1a3fe886BdCB5cD01e9f6BDd8633`,
   upto `0x8c819E6De3df83E0e87bBE7651c5D4e83229b239`; signer
   `0x34F7a661160780Ce1346e6D7B96D2bE244590899` (all in
   `CURATED_B402_SPENDERS` / dated).
2. **Which contract is b402's testnet eip3009 `$U` (domain name "U")?** The curated
   `('bsc-testnet','U')` entry now pins `0xC70b…5565` (domain "United Stables"/"1"
   VERIFIED via DOMAIN_SEPARATOR reconstruction; holds the circulating test funds) —
   which still mismatches the `/supported` kind name ("U"), so testnet b402
   authorization settles remain impossible until the b402 contact names the true
   "U"-domain contract. The formerly curated `0x180B…6A49` (facilitator-gated,
   domain unreadable) was de-curated 2026-07-02.
3. **Does b402 key permit2 assets by address or by `extra.name`?** For settle we send
   `asset` explicitly; confirm `extra.name` is advisory for permit2 kinds (it does not
   enter the signature).
